import React from 'react'
import ReactDOM from 'react-dom/client'
import CriticalSparesApp from '../../src/spares/CriticalSparesApp'
import '../../src/styles.css'

ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><CriticalSparesApp /></React.StrictMode>)
